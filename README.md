# TinyURL Clone

Sistema de acortador de URLs con:
- Backend: Node.js + TypeScript + Express
- Frontend: Angular
- Base de datos: PostgreSQL (Docker)

Arranque rápido (una vez instalado Node, npm, Angular CLI y Docker):

```bash
# Desde la raíz del proyecto
docker compose up --build
```
